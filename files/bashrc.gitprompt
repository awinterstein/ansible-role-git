if [ -f "$HOME/.bash_git_prompt/gitprompt.sh" ]
then

    GIT_PROMPT_ONLY_IN_REPO=1
    GIT_PROMPT_SHOW_UPSTREAM=0
    GIT_PROMPT_START=
    GIT_PROMPT_THEME=Custom

    source "$HOME/.bash_git_prompt/gitprompt.sh"

fi